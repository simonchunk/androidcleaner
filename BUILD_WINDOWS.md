# Windows self-contained build
This build uses PyInstaller on a Windows GitHub Actions runner. It produces AndroidCleaner.exe with Python bundled, so store PCs do not install or download Python.

Run the `Build Windows Android Cleaner` workflow in GitHub Actions, then download the generated artifact.
