# v1.2.13 Local Test

- Fixes the current icon blocker from resolver(7): `adb_run` was undefined.
- icon-helper.jar can now be pushed and the Android app_process renderer can execute.
- New red The iPhone Guy branding artwork in the header.
- Removes the native-looking Treeview border and grey ttk scrollbar.
- Uses red/orange/yellow risk indicators plus stronger row tinting.
- Compacts App Assessment so the bottom action buttons stay visible.
- Keeps a useful 92px selected-app icon without wasting the right panel.
