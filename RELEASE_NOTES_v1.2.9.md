# v1.2.9 Local Test — Actual Treeview icon hook

Hooks icon discovery directly into the current UI method that actually inserts visible Treeview rows: `apply_view()`.

New log chain:
BUILD MARKER Android Cleaner v1.2.9 Treeview Icon Hook loaded
ICON TREEVIEW HOOK method=apply_view
ICON UI TRIGGER
ICON PIPELINE START
ICON PIPELINE QUEUE
ICON DEVICE ...

The BUILD MARKER also proves the installed EXE contains/runs this build.
